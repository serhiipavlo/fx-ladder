// Placeholder until the first perf gate is armed (v0.1.0). Exists so the
// §2.4 command set is complete and green from the first task onwards.
console.log('perf gate: no thresholds armed yet — the first gate ships with v0.1.0.');
process.exit(0);
