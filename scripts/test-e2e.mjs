// Placeholder until the first Playwright spec lands (v0.1.0). Exists so the
// §2.4 command set is complete and green from the first task onwards.
console.log('e2e: no specs yet — the first Playwright spec ships with v0.1.0.');
process.exit(0);
