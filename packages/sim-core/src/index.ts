// Stub entry — seeded PRNG and the market model behind advance(now) arrive with v0.1.0.
// This package stays pure: no Node imports, no DOM, no timers, no ambient time or randomness.
export const PKG = '@fx/sim-core';
